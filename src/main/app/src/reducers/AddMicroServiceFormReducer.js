const microServiceFormReducer = ( state={}, action) =>{
  switch (action.type) {
    case "handleSubmit":
      return state
      break;
    default:
      return state

  }
}

export default microServiceFormReducer
